name: Code Quality

on:
  pull_request:
    branches: [main]

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  quality:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Lint
        run: npm run lint

      - name: Build
        run: npm run build

      - name: Test
        run: npm run test:coverage

  # test:
  #   needs: quality
  #   runs-on: ubuntu-latest
  #   steps:
  #     - name: Checkout
  #       uses: actions/checkout@v4

  #     - name: Setup Node.js
  #       uses: actions/setup-node@v4
  #       with:
  #         node-version: '22'
  #         cache: 'npm'

  #     - name: Install dependencies
  #       run: npm ci

  #     - name: Set up Docker Buildx
  #       uses: docker/setup-buildx-action@v3

  #     - name: Install k3d
  #       run: curl -s https://raw.githubusercontent.com/k3d-io/k3d/main/install.sh | bash

  #     - name: Install Helm
  #       uses: azure/setup-helm@v4

  #     - name: Run Docker integration tests
  #       run: npm run test:integration -- tests/integration/docker-client.test.ts
  #       env:
  #         INTEGRATION_TESTS: 'true'

  #     - name: Run Kubernetes integration tests
  #       run: npm run test:integration -- tests/integration/kubernetes-client.test.ts
  #       env:
  #         INTEGRATION_TESTS: 'true'

  #     - name: Run Docker E2E tests
  #       run: bash tests/e2e/docker/run.sh

  #     - name: Run Kubernetes E2E tests
  #       run: bash tests/e2e/kubernetes/run.sh
