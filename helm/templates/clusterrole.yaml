{{- if .Values.rbac.create -}}
{{- if not .Values.rbac.existingClusterRoleName -}}
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: {{ include "containrdog.fullname" . }}
  labels:
    {{- include "containrdog.labels" . | nindent 4 }}
rules:
  - apiGroups: [""]
    resources: ["pods"]
    verbs: ["get", "list"]
  - apiGroups: ["apps"]
    resources: ["replicasets"]
    verbs: ["get"]
  - apiGroups: ["apps"]
    resources: ["deployments", "statefulsets", "daemonsets"]
    verbs: ["get", "list", "patch"]
{{- with .Values.rbac.extraRules }}
  {{- toYaml . | nindent 2 }}
{{- end }}
{{- else if .Values.rbac.extraRules }}
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: {{ include "containrdog.fullname" . }}-extra
  labels:
    {{- include "containrdog.labels" . | nindent 4 }}
rules:
  {{- toYaml .Values.rbac.extraRules | nindent 2 }}
{{- end }}
{{- end }}
